const data = [
    { id: 1, name: "Gaurav Choudhary" },
    { id: 2, name: "Jasprit" },
    { id: 3, name: "Harkirat" }
];

const dataIds = data.map((data) => {
    return data.name;
});

console.log(dataIds);